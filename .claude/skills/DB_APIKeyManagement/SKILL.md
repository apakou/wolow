Description: Provides guidance and code examples for securely handling Supabase API keys in a development environment.

• Purpose: To prevent exposure of sensitive API keys and adhere to security best practices.

• Inputs: projectType (e.g., Next.js, React), keyType (e.g., anon, service_role).

• Outputs: Instructions and code snippets for environment variable setup and access.

• Workflow:

1. Explain the difference between public and secret keys.

2. Provide examples of .env.local configuration.

3. Show how to access keys in client-side and server-side code.
